# ── Web framework ──────────────────────────────────────────────────────────
flask>=3.0.0
fla>=3.1.0
werkzeug>=3.0.0

# ── AI providers ───────────────────────────────────────────────────────────
openai>=1.30.0
groq>=0.9.0

# ── Multi-agent orchestration (Agno / phidata) ─────────────────────────────
agno>=1.0.0          # or: phidata>=2.5.0  — whichever version you installed

# ── RAG pipeline ───────────────────────────────────────────────────────────
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-huggingface>=0.1.0,<0.2.0
langchain-chroma>=0.1.4,<0.2.0
langchain-text-splitters>=0.3.0,<0.4.0
chromadb>=0.5.0,<0.7.0
sentence-transformers>=3.0.0,<4.0.0

# ── YouTube ────────────────────────────────────────────────────────────────
youtube-transcript-api>=0.6.2   # fetch captions from YouTube videos
yt-dlp>=2024.1.0                # fallback: extract video metadata & title

# ── Document loaders ───────────────────────────────────────────────────────
pypdf>=4.0.0          # PDF parsing
unstructured>=0.14.0  # optional, richer document support

# ── Web search tool (DuckDuckGo) ────────────────────────────────────────────
duckduckgo-search>=6.0.0

# ── Utilities ──────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0.0
markdown>=3.6.0
requests>=2.31.0

# ── Optional: speed up HuggingFace on CPU ─────────────────────────────────
# torch>=2.0.0       # uncomment if you want GPU/CPU tensor acceleration